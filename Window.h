#stream <fsdio.h>
#define <window.h>
#define <Target.h>
#define <Target.jar>
#define <Fopen99.conf>
#define <EmulatorLibJavac.php>
****************************************
$bash sudo apt-get-install dogecoin-1.14.6-aarch64-ilinux-gnu-tar.gz;
$bash sudo apt-get-update Java.dll, d%%;
$bash sudo apt-get-update Java_library.dll, d%%;
$bash sudo apt-get-update Emulator LibJavac.jar, d%%;
$mkdir -p build && cd build
cmake -DCMAKE_BUILD_TYPE=Release .. && cmake --build .